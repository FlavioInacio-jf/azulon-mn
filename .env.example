# Piece weights by element (values must be between 0.0 and 1.0)
# Use dot as decimal separator (e.g., 0.75)

WATER_WEIGHT=0.50
EARTH_WEIGHT=0.50
FIRE_WEIGHT=0.50
AIR_WEIGHT=0.50
EMPTY_WEIGHT=0.00

# Optional: default minimax depth (only if your project supports it)
# MINIMAX_DEPTH=3
